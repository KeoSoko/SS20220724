# Investigate Antebellum Paystack Renewal History — READ ONLY

Do not modify production.

Do not charge the customer.

Do not create checkout.

Do not recover or attach a subscription identity yet.

I have manually inspected this customer's Paystack dashboard and found information that contradicts our earlier assumption that no August renewal was attempted.

The visible Paystack history is:

* 26 Apr 2026 — ZAR 49 — Apple Pay — successful
* 26 May 2026 — ZAR 49 — Card — unsuccessful
* 26 Jun 2026 — ZAR 49 — Card — unsuccessful
* 2 Jul 2026 — ZAR 49 — Apple Pay — successful
* 26 Jul 2026 — ZAR 49 — Card — unsuccessful
* 2 Aug 2026 — ZAR 49 — Card — abandoned

The August 2 transaction detail shows:

* Amount: ZAR 49
* Status: Abandoned
* Channel: Card
* Plan: Simple Slips Recurring R49 Subscription - Monthly
* Message: "The transaction was not completed"

This means we must re-investigate the customer's provider relationship before using Task #46 recovery.

Perform a read-only investigation and report:

1. The Paystack customer code and provider customer ID.

2. Every Paystack subscription (`SUB_*`) associated with this customer, including:

   * subscription code;
   * plan code;
   * status;
   * created date;
   * next payment date;
   * authorization relationship where safely available.

3. Every Paystack invoice/renewal attempt associated with those subscriptions from April through August 2026.

4. For each of the six visible transactions above determine:

   * transaction reference;
   * transaction ID;
   * channel;
   * status;
   * gateway response;
   * whether it belongs to a Paystack subscription;
   * subscription code;
   * invoice code if applicable;
   * whether it was an initial/customer-initiated checkout or recurring provider attempt.

5. Determine why the successful transactions occurred through Apple Pay while subsequent recurring attempts appear as Card.

6. Determine whether the Apple Pay authorization created by the successful payment is reusable for recurring Paystack subscription charges.

Do not infer this from metadata alone. Use authoritative Paystack provider information.

7. Specifically investigate the August 2 abandoned transaction.

Determine whether:

* Paystack initiated it automatically as a recurring subscription renewal;
* the customer manually initiated it;
* authentication/customer action was required;
* Paystack attempted a reusable authorization;
* the bank/provider rejected it;
* it was abandoned before authorization;
* or the available provider evidence is insufficient to determine why.

Do not label it "declined" unless Paystack explicitly proves a decline.

8. Compare all of this against Simple Slips production:

* local subscription;
* next billing date;
* Paystack customer code;
* local `paystack_subscription_identities`;
* `payment_transactions`;
* `billing_events`;
* renewal reconciliation events.

9. Determine whether Simple Slips failed to record an existing legitimate `SUB_*` relationship.

10. Determine whether Task #46 would currently classify this customer as:

* payment_failed;
* reconciling;
* renewal_setup_required;
* recovered provider identity.

11. Determine whether that classification is correct given the newly discovered August 2 Paystack transaction.

12. Most importantly:

**Do not allow a new recovery checkout until we have established whether an existing Paystack subscription relationship already exists.**

Return a chronological timeline:

`Customer action → Paystack subscription/invoice → transaction → Simple Slips local state`

for April through August.

End with:

`SAFE TO USE TASK #46 RECOVERY`

or

`DO NOT START NEW CHECKOUT — [reason]`

No production mutation.
