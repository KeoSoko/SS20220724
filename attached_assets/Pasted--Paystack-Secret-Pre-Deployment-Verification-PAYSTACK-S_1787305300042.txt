# Paystack Secret Pre-Deployment Verification

`PAYSTACK_SECRET_KEY` has now been added through Replit's managed Secrets UI.

Do not print, log, partially reveal, hash, or otherwise expose the key.

Do not deploy yet.

## 1. Confirm environment availability

Report only:

* Development runtime: `AVAILABLE` / `NOT AVAILABLE`
* Production deployment: `AVAILABLE` / `NOT AVAILABLE`
* Agent/task environment: `AVAILABLE` / `NOT AVAILABLE`

Do not reveal the value.

Production must be `AVAILABLE`.

## 2. Audit all Paystack secret usage

Search the current codebase for every environment variable used for Paystack authentication.

Report variable NAMES only.

Specifically determine whether historical code uses any alternate names such as:

* `PAYSTACK_SECRET`
* `PAYSTACK_SECRET_KEY`
* `PAYSTACK_PRIVATE_KEY`
* other Paystack secret variables

Do not expose values.

Explain how the existing production application previously performed:

* transaction verification;
* webhook signature verification;
* subscription lookup/reconciliation;
* any other authenticated Paystack API calls;

if `PAYSTACK_SECRET_KEY` was previously absent.

If another live secret variable already exists, STOP and explain before changing architecture or duplicating credentials.

## 3. Verify server Paystack client initializes

Without performing a charge or creating a subscription, confirm that the backend now initializes the Paystack client successfully from:

`PAYSTACK_SECRET_KEY`

Do not make a mutating Paystack call.

If a safe read-only/provider validation is unavailable, source/configuration verification is sufficient.

## 4. Confirm production configuration

Verify:

`PAYSTACK_APPLE_PAY_SUBSCRIPTIONS_ENABLED=false`

and:

`PAYSTACK_SECRET_KEY = AVAILABLE`

Do not expose either secret value.

## 5. Migration readiness

Confirm the only pending billing schema change is:

`0005_add_paystack_access_code`

Expected:

`ADD COLUMN paystack_access_code text`

No destructive changes.

## 6. Final decision

End with exactly:

`READY TO PUBLISH SERVER-SIDE PAYSTACK INITIALIZATION`

or:

`DO NOT PUBLISH — [blocker]`

Do not publish.
