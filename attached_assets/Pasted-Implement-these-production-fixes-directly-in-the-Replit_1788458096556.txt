Implement these production fixes directly in the Replit workspace now (not just a plan). Do not modify production customer data, delete the temporary smoke-test account, or initiate billing.

1. Registration integrity: export one shared strong password schema (8-64 chars, lowercase, uppercase, number, special character) from shared/schema.ts and use it in both client/src/pages/auth-page.tsx and server/auth.ts. Trim username and normalize email. Require agreedToTerms === true and agreedToTaxDisclaimer === true on the server. Send both booleans from the client and type the registration mutation accordingly. Record a registration_legal_acceptance billing event in the existing billing_events table with acceptedAt and version 2026-09-03; do not add a database migration.
2. Trial integrity: remove the second registration call to storage.startFreeTrial. Create only one user_subscriptions trial. Calculate one trialStartDate and exact trialEndDate, then mirror that same end date to users.trial_end_date. Record trial_started. In BillingService.startFreeTrial, mirror the authoritative expiry to users. During late email-verification trial restart, update both user_subscriptions.trial_end_date and users.trial_end_date to the same value.
3. Verification: if an already-verified account still has a verification token or missing verified timestamp, clear/fill those fields. In client/src/pages/verify-email-page.tsx, do not treat invalid/expired tokens as success; show an error and allow resend. Say users can sign in with email and password.
4. Startup: in server/index.ts explicitly await initializeDatabase() after optional Azure initialization and before subscription-plan/billing initialization; fail clearly if retries return false. Remove the background initialize() call from DatabaseStorage's constructor so startup does not race two database probes.
5. Tests: add/update focused registration integrity and startup-health tests. Run TypeScript check, focused tests, and production build. Preserve all unrelated changes. Report the exact files changed and test results, and stop before publishing.