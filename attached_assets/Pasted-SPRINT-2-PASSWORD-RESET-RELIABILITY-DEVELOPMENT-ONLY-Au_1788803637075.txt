SPRINT 2: PASSWORD-RESET RELIABILITY — DEVELOPMENT ONLY. Audit, repair, and test the complete forgot/reset-password journey. Preserve existing branding, onboarding, authentication, billing, email-verification, receipt, and production behavior. Do not access or mutate production and do not publish.

Requirements:
1. Trace forgot-password request, token generation/storage, email construction, reset-link route/page, password update, token invalidation, and post-reset navigation.
2. Security: forgot endpoint must return a neutral response for existing and nonexistent emails; no account enumeration; cryptographically strong opaque tokens; store safely (prefer hash if architecture permits without unsafe migration); explicit finite expiry; single use; newest request invalidates older tokens; rate-limit resend/request; never log tokens/links/passwords; prevent open redirects; password-reset endpoint must work while logged out and must not be blocked by an absent or expired JWT in an Authorization header.
3. Environment links: use the existing strict public-origin resolver. Development must use current REPLIT_DEV_DOMAIN; production must remain exactly https://simpleslips.app. Never trust Host/X-Forwarded headers.
4. UX: clear branded Forgot Password and Choose New Password screens, password requirements visible before submission, confirm-password matching, accessible errors/loading/success, mobile-safe layout. After success, clear stale auth state and send user to /auth?mode=signin Welcome back with a concise success message. Do not auto-save passwords.
5. Verification semantics: password reset must not mark email verified or change subscriptions/access. Decide and document whether unverified accounts may reset; preserve the intended rule unless unsafe.
6. Add focused server/client/security tests covering valid reset, invalid/expired/reused/superseded token, logged-out request, expired JWT header, unknown email neutrality, rate limit, weak/mismatched passwords, environment origin, post-success route, and no verification/billing mutation.
7. Run focused tests, TypeScript, full production build, diff validation, restart development, inspect logs. Use fixtures/transactions/mocks only; do not send a real email, change notifications@simpleslips.co.za, create persistent users, or invoke providers/payment systems.
8. Report root causes, changes, pass/fail counts, limitations, and exact manual test steps. Do not publish.